<div class="span11 forum">
      <h2>The Climbing Portal Forum</h2>
</div>

<table>
  <tr>
    <th>Title</th>
    <th>Content</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.title %></td>
    <td><%= post.content %></td>
    <td><%= link_to 'Show', post %></td>
    <!-- <td><%= link_to 'Edit', edit_post_path(post) if user_signed_in? %></td> -->
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_post_path if user_signed_in? %>
